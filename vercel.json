{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "react",
  "env": {
    "BUYPIX_API_KEY": {
      "description": "Sua chave de API BuyPix (comece com bpx_live_)",
      "required": true
    },
    "WEBHOOK_URL": {
      "description": "URL do webhook (será gerada automaticamente pelo Vercel)",
      "required": false
    },
    "BUYPIX_WEBHOOK_SECRET": {
      "description": "Secret para validar webhooks (opcional)",
      "required": false
    }
  }
}
